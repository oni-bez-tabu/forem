<%= render partial: "content_preferences" if current_user.base_subscriber? %>

<%= render partial: "response_templates" %>

<%= render partial: "publishing_from_rss" %>

<%= render partial: "api_keys" if current_user.trusted? %>