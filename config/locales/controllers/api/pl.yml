pl:
  api:
    v0:
      analytics_controller:
        invalid_date_format: Parametry daty 'start' lub 'end' muszą być w formacie 'yyyy-mm-dd'
        start_missing: Brakuje wymaganego parametru 'start'
      articles_controller:
        must_be_json: parametr article musi być obiektem JSON. Przekazano article jako %{type}
      follows_controller:
        followed:
          one: zaobserwowano %{count} użytkownika
          few: zaobserwowano %{count} użytkowników
          many: zaobserwowano %{count} użytkowników
          other: zaobserwowano %{count} użytkowników
      health_checks_controller:
        app_is_up: Aplikacja działa!
        database_connected: Baza danych połączona
        database_not_connected: Baza danych NIE jest połączona!
        redis_connected: Redis połączony
        redis_not_connected: Redis NIE jest połączony!
      listings_controller:
        no_credit: Brak wystarczającej liczby dostępnych kredytów
